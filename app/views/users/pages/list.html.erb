<div class="container">
  <h1 class="title is-2">Usuários Cadastrados</h1>

  <table class="table is-bordered">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Equipe</th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name_or_email %></td>
          <td><%= user.squad.name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
